# QUARANTINED — historical Mostly Armless context attachment

This file previously carried proprietary Mostly Armless/Serenity material into
the LPS documentation tree. It is intentionally retained only as a quarantine
marker so prior references resolve; its prior contents have been removed.

It must not be loaded as LPS agent context, used as a specification, converted
into a prompt or harness rule, or treated as an LPS architectural dependency.
See `docs/architecture/REFERENCE_MATERIAL_BOUNDARY.md`.
