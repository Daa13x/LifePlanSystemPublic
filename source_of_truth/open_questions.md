# Open Questions

This public file tracks project and architecture questions only.

## UI questions

- Should the UI read from GitHub directly, a local clone, or both?
- Should writes create staged files, pull requests, or direct commits after approval?
- Should the first UI be local-only or web-based?
- Which model providers should be supported first?
- What belongs in the first MVP?
- How should protected files be hidden or permission-gated?

## Architecture questions

- Should reasoning calibration live in source-of-truth or generated review reports?
- Should advice usefulness be tracked as structured data?
- Should source review notes live under `docs/source_review/`, `books/`, or another folder?
- What is the smallest useful public demo workflow?
